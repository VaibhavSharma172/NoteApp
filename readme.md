Note = mongoose model
notes = collection name in mongodb
noteSchema = schema object 
/notes = route
getNotes, addNote, patchNote, deleteNote = controller function
authRoutes = router imported into index.js from routes.js. groups all note‑related endpoints under /api (???)
